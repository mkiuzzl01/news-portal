import NewsCard from "./NewsCard";

const International = () => {
  return (
    <div className="container m-auto flex lg:flex-row flex-col justify-between">
      {/* here add all section for ui */}
      <NewsCard />
    </div>
  );
};

export default International;
